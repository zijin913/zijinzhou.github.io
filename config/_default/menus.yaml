# Navigation Links
#   Single-page layout: nav intentionally empty so the topbar shows only the
#   site name and language toggle. Everything lives on the homepage; project
#   detail pages are still reachable via the showcase cards.

main: []
